<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title><%=title %></title>
	<link rel="stylesheet" href="./index.css" />
</head>
<body>
	<h1>CSS Challenges</h1>
	<ul>
		<% files.forEach(function(file, index) {%>
			<li><a href="<%=htmlRelative + '/' + file + '/index.html' %>"><%=file %></a></li>
		<% })%>
	</ul>
</body>
</html>